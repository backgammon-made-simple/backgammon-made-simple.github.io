# Project tool commands

On Windows, run project tools through `scripts/codex-tools.ps1`; do not rely on
inherited `PATH`, activate `.venv`, or invoke bare `node`, `quarto`, `Rscript`,
or `bash` commands. Use the documented `verify`, `browser-contract`, `quick`,
`comprehensive`, and `preview` launcher commands.

